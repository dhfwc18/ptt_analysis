# test/__init__.py
"""Test suite for the PTT Crawler project."""

# External imports
import sys
from pathlib import Path

# Ensure the src directory is in the path for imports
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
